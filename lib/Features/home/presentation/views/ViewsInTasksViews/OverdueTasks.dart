import 'package:flutter/material.dart';

class OverdueTasksView extends StatelessWidget {
  const OverdueTasksView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('OverdueTasksView Tasks'),
      ),
    );
  }
}