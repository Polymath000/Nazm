import 'package:flutter/material.dart';

class TomorrowTasksView extends StatelessWidget {
  const TomorrowTasksView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('TomorrowTasksView Tasks'),
      ),
    );
  }
}