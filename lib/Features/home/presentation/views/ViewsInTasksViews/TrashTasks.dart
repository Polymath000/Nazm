import 'package:flutter/material.dart';

class TrashTasksView extends StatelessWidget {
  const TrashTasksView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('TrashTasksView Tasks'),
      ),
    );
  }
}